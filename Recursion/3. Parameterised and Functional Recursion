// Calculate sum of first N numbers;

// Parameterized way
void sum(int i, int sum){
  if(i<1){
    cout<<sum;
    return;
  }
  sum(i-1, sum+i)
}

// Functional way
int sum(int n){
  if(n==0)
    return 0;
  return n+sum(n-1);
}
