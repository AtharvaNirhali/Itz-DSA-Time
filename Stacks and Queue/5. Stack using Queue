// using inbuild queue functions

class st{
  queue<int> q;

  void Spush(int x){
    int s=q.size;
    q.push(x);
    for(int i=0; i<s; i++){
      q.push(q.top());
      q.pop();
    }
  }

  int Spop(){
    return q.pop();
  }

  int Stop(){
    return q.top()
  }
  
};
